apiVersion: v1
kind: Service
metadata:
  name: webapp
  labels:
    app: webapp
spec:
  type: ClusterIP        # no cloud LoadBalancer -> no ELB cost; reach it via port-forward
  selector:
    app: webapp
  ports:
  - port: 80
    targetPort: 80
